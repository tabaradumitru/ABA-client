export enum LocalStorageKeys {
  USER = 'user'
}

export enum Role {
  User = 'User',
  Employee = 'Employee'
}

export enum RequestStatuses {
  Waiting = 1,
  Approved,
  Rejected,
  Canceled,
  Expired
}
